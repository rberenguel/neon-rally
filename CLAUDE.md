# Neon Rally — Project Notes

## Dev Server
Never start a dev server or any serving process (vite, http-server, npx serve, etc.).
Only the user starts the server. To test changes, tell the user to refresh their browser.
